# Visit https://github.com/lowlighter/metrics#-documentation for full reference
name: Metrics
on:
  # Schedule updates (each hour)
  schedule: [{cron: "0 0 */2 * *"}]
  # Lines below let you run workflow manually and on each commit
  workflow_dispatch:
  push: {branches: ["master", "main"]}
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          # Your GitHub token
          # The following scopes are required:
          #  - public_access (default scope)
          #  - read:user
          #  - read:org
          # The following additional scopes may be required:
          #  - read:org      (for organization related metrics)
          #  - read:user     (for user related data)
          #  - read:packages (for some packages related data)
          #  - repo          (optional, if you want to include private repositories)
          token: ${{ secrets.METRICS_TOKEN }}

          # Options
          user: bl4ckswordsman
          template: classic
          base: header, activity, repositories, metadata
          config_octicon: yes
          config_timezone: Europe/Stockholm
          extras_js: |
            const labelsToHide = [
              "Pull requests reviewed",
              "Pull requests opened",
              "Issues opened",
              "Issue comments",
              "Releases",
              "Watchers",
              "Sponsors",
            ];

            document.querySelectorAll("text").forEach(el => {
              const content = el.textContent.trim();
              const match =
                labelsToHide.some(label => content.includes(label)) ||
                /Prefers .+ license/.test(content);
              if (!match) return;

              // Remove ONLY the immediate parent <g> = one row, not the whole section
              const parent = el.parentElement;
              console.log(parent?.outerHTML?.slice(0, 200))
              if (parent) parent.remove();
            });

          plugin_languages: yes
          plugin_languages_ignored: html, cmake
          plugin_languages_analysis_timeout: 15
          plugin_languages_analysis_timeout_repositories: 7.5
          plugin_languages_categories: markup, programming
          plugin_languages_colors: github
          plugin_languages_limit: 8
          plugin_languages_recent_categories: markup, programming
          plugin_languages_recent_days: 14
          plugin_languages_recent_load: 300
          plugin_languages_sections: most-used
          plugin_languages_threshold: 0%
          plugin_lines: no
          plugin_notable: yes
          plugin_notable_from: all
          plugin_notable_repositories: yes
          plugin_notable_types: commit
          repositories_forks: yes
